apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mediawiki-chart-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mediawiki-chart-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mediawiki-chart-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
